<!DOCTYPE html>
<html>
    <head>
        {{ template "header" }}
        <script src="/web/css/sortable.min.js"></script>
        <link rel="stylesheet" type="text/css" href="/web/css/tracks.css" />
        <style>
            h1 {
                text-align: center;
            }

            p {
                text-align: center;
            }
        </style>
    </head>
    <body>
        {{ template "topnav" }}
        <div class="content">
            <h1>Recommendations</h1>
            <p>To receive fresh recommendations, reload the page.</p>
            {{ template "tracks" . }}
        </div> 
        {{ template "footer" }}
    </body>
</html>